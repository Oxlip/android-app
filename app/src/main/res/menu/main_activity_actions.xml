<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/main_action_bar_progress"
        android:checkable="false"
        android:orderInCategory="1"
        android:showAsAction="ifRoom"/>

    <item android:id="@+id/main_action_bar_scan"
        android:icon="@drawable/ic_launcher"
        android:title="@string/main_action_bar_scan"
        android:showAsAction="never" />

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/main_action_bar_settings"
        android:title="@string/main_action_bar_settings"
        android:showAsAction="never" />
</menu>